DIM n AS INTEGER
n = 1
IF n > 0 THEN
    IF n > 1 THEN
        IF n > 2 THEN
            IF n > 3 THEN
                IF n > 4 THEN
                    IF n > 5 THEN
                        PRINT "deep"
                    END IF
                END IF
            END IF
        END IF
    END IF
END IF
