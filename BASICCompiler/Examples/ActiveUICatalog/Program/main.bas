' The ActiveUI catalog, in BASIC — the measurement first.
'
' Before any page is written, this asks what ActiveUI gives a BASIC program:
'
'   basicc import-report main.bas    ' what imported, and what did not
'   basicc import-probe  main.bas    ' compiles every shim; the coverage gate
'
IMPORT "ActiveUI"

PRINT "ActiveUI imported"
